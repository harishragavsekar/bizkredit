package com.bizkredit.common.enums;

// Underwriting manager's final decision on a credit proposal
public enum DecisionStatus {
    APPROVED,
    DECLINED,
    CONDITIONAL_APPROVAL    // Approved with specific conditions
}
